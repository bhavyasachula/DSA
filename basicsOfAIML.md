Ai = ai is technology that enables machine to perform the tasks that requires the human intelligence such as decision making and problem solving

ml = is a subset of ai that is trained on huge amount of data and make prediction without being explicitly told or programmed

dl = deep learnning is subset of machine learning where it uses neural networks with multple layers to learn complex patterns from the large datasets

llm = is a deep learning model trained on vast amount of data to understand and generate the human like language and also can contiues the conversation , can write content , summarize the info and generate the code

OCR Optical character recognition :

1)image preprocessing:
converts image to grayscale
remove noise 
increase the contrast
correct rotation/skew

2)text recognition:
ml models recognitions the text region and ignores the logos 
,images and backgrounds

character recognition:
ocr model looks at the shape of the letter and character 
by using cnn which learns visual patterns fromt he millions of text images
extracts like 
edges curves corners shapes

ocr flow:
image
preprocessing
text recognition
character recognition 
neural networks 
character prediction
text output
 